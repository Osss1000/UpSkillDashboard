@model UpSkillDashboard.Models.Organization

@{
    ViewData["Title"] = "Create Organization";
}

<div class="container px-4">
    <h2 class="text-primary">Create Organization</h2>

    <div class="card shadow p-4">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Name</label>
                <input name="name" class="form-control shadow-sm" required />
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Email</label>
                <input name="email" type="email" class="form-control shadow-sm" required />
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Phone Number</label>
                <input name="phoneNumber" class="form-control shadow-sm" required pattern="^\+?[0-9]{10,15}$" />
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Organization Name</label>
                <input name="organizationName" class="form-control shadow-sm" required />
            </div>

            <div class="form-group">
                <label asp-for="OrganizationRole">Organization Type</label>
                <select asp-for="OrganizationRole" asp-items="Html.GetEnumSelectList<OrganizationRoleEnum>()" class="form-control"></select>
            </div>

            <button type="submit" class="btn btn-success mt-3">Create</button>
        </form>
    </div>
</div>
